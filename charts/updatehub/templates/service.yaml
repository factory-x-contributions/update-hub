apiVersion: v1
kind: Service
metadata:
  name: {{ include "updatehub.fullname" . }}
  labels:
    {{- include "updatehub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "updatehub.selectorLabels" . | nindent 4 }}
